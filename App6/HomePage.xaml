<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App6.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App6"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0"
            Padding="12"
            Orientation="Vertical"
            Margin="30,20"
            Spacing="15">
        <StackPanel Orientation="Horizontal" Spacing="10">
            <Button
                Click="Button_Click_Game"
                Content="Game Start"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                Click="Button_Click"
                Content="Kill Process"
                Style="{StaticResource SubtleButtonStyle}" />
        </StackPanel>
        <TextBox
                x:Name="Path"
                Margin="0,20,0,0"
                Header="Genshin Impact Game Path:"
                PlaceholderText="eg: C:\\Genshin Impact\\YuanShen.exe" />
        <SelectorBar x:Name="SelectorBar2" SelectionChanged="SelectorBar2_SelectionChanged">
            <SelectorBarItem x:Name="SelectorBarItemPage1" Text="Game Options" IsSelected="True" />
            <SelectorBarItem x:Name="SelectorBarItemPage2" Text="Injection Options" Margin="20,0"/>
        </SelectorBar>
        
        

    </StackPanel>
        <Frame x:Name="contentFrame" Grid.Row="1"/>
    </Grid>
</Page>
