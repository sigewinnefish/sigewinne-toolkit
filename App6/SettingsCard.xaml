<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="App6.SettingsCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App6"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Margin="4,2,4,2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4" BorderThickness="0.5"
            BorderBrush="{ThemeResource SystemListLowColor}">

        <Grid x:Name="RootGrid" Padding="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

        <FontIcon x:Name="Icon" Glyph="&#xE702;" FontSize="24" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0"/>


            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock x:Name="TitleText" Text="HDR" FontSize="16" />
                    <TextBlock x:Name="SubtitleText" Text="可发现为 “DESKTOP-XXXXXX”"
                       FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                <ToggleSwitch Grid.Column="1" IsOn="False" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>

        
    </Grid>
    </Border>

</UserControl>
